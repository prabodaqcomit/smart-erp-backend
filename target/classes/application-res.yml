# This is a sample command please replace with correct command
spring:
  datasource:
    url: jdbc:mysql://localhost:3309/smart_erp_res
    username: root
    password: 1234
#    hikari:
#      schema: systempg

  jpa:
    hibernate:
      ddl-auto: update
eurek:
  client:
    enabled: false

jwt:
  token:
    validity: 18000
    prefix: Bearer
  signing:
    key: signingkey
  authorities:
    key: roles
  header:
    string: Authorization

server:
  port: 8003
  error:
    include-message: always